/*
 * define an array and insert values and print it
 */
testArr = [ 1, 2, 3, 4, 5]

testArr.push(6)

console.log(testArr);

for (let i = 0;  i < testArr.length; i++) {
    console.log(testArr[i]);
}
